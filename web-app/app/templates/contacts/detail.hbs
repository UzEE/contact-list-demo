{{#basic-card title=model.displayName}}

  {{#block-slot 'action-buttons'}}
  <button class="btn btn-danger" {{action "deleteContact" model}}>
    <span class="glyphicon glyphicon-trash" aria-hidden="true"></span> <span class="text">Remove</span>
  </button>
  {{/block-slot}}

  {{#block-slot 'body'}}
  <div class="container-fluid xs-container">
    <div class="row">

      <section class="col-xs-12 col-sm-4 col-md-3 pull-right">

        <div class="thumbnail">
          <img src="{{model.getAvatarUrl}}" title={{model.name}} />
          
        </div>

      </section>

      <section class="col-xs-12 col-sm-8 col-md-9">

        {{contact-editor 
            model=model 
            allowEdit=true
            isEditMode=isEditMode
        }}
    
      </section>

    </div>
  </div>
  {{/block-slot}}

{{/basic-card}}


{{!--{{#link-to "contacts.edit" model class="btn btn-primary"}}
  <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Edit Contact
{{/link-to}}--}}

{{outlet}}
